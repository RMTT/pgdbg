# pgdbg

## Dev environment tips

+ don't run the python scripts directly cause it needs gdb library which only can be used inside gdb

## Testing instructions

+ use `source {file_name}` inside gdb to test scripts
